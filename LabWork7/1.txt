# Открываем файл для чтения
with open('file.txt', 'r') as file:
    # Читаем файл построчно
    for line in file:
        # Разбиваем строку по пробелам и преобразуем в целые числа
        numbers = list(map(int, line.split()))
        # Вычисляем сумму чисел в строке
        print(sum(numbers))
